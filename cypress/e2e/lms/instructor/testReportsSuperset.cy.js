describe('[TC_AUTHOR_SUMAC_1] Course Comparison Dashboard in Superset for Staff/Superuser', () => {
  it.skip('active users with Staff and/or Superuser django permissions can view the Course Comparison Dashboard in Superset', () => {
  })
})

describe('[TC_AUTHOR_SUMAC_2] Course Comparison Dashboard in Superset data for different user roles', () => {
  it.skip('course admin access the Course Comparison Dashboard to compare their courses', () => {
  })

  it.skip('course staff access the Course Comparison Dashboard to compare their courses', () => {
  })

  it.skip('limited staff access the Course Comparison Dashboard to compare their courses', () => {
  })

  it.skip('data researcher does not have access to Superset', () => {
  })
})

describe('[TC_AUTHOR_SUMAC_3] Access to Superset for different roles', () => {
  it.skip('Course Admin log into Superset', () => {
  })

  it.skip('Course Staff log into Superset', () => {
  })

  it.skip('Limited Staff log into Superset', () => {
  })
})

describe('[TC_AUTHOR_SUMAC_4] Course Comparison Dashboard - enrollee counts', () => {
  it.skip('when a new learner enrolls in a course, the enrollee counts increase in the Course Comparison Dashboard', () => {
  })
})

describe('[TC_AUTHOR_SUMAC_5] Course Comparison Dashboard - video engagement information', () => {
  it.skip('video engagement information updates in the Course Comparison Dashboard as expected when video information changes for the course', () => {
  })
})

describe('[TC_AUTHOR_SUMAC_6] Course Comparison Dashboard - problem engagement information', () => {
  it.skip('problem engagement information updates in the Course Comparison Dashboard as expected when problem engagement changes for the course', () => {
  })
})

describe('[TC_AUTHOR_SUMAC_7] Course Comparison Dashboard - course-level tags', () => {
  it.skip('course-level tags are shown and the Course-level tag filter works in the Course Comparison Dashboard', () => {
  })
})

describe('[TC_AUTHOR_SUMAC_8] Course Comparison Dashboard - course and Course Runs', () => {
  it.skip('course and Course Runs display in the Course Comparison Dashboard as expected and Course Run filter works', () => {
  })
})

describe('[TC_AUTHOR_SUMAC_9] Course Comparison Dashboard - course filter', () => {
  it.skip('course filter works in the Course Comparison Dashboard', () => {
  })
})

describe('[TC_AUTHOR_SUMAC_10] Course Comparison Dashboard - links to course dashboard work', () => {
  it.skip('links to course dashboard work', () => {
  })
})
