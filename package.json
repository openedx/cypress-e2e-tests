{
  "name": "cypress-e2e-tests",
  "version": "1.0.0",
  "description": "Cypress E2E Tests for edX applications",
  "repository": "https://github.com/edx/cypress-e2e-tests",
  "scripts": {
    "cy:open": "cypress open --env configFile=journals_stage",
    "cy:run": "cypress run --env configFile=journals_stage",
    "lint": "eslint --ext .js --ext .jsx .",
    "precommit": "npm run lint"
  },
  "dependencies": {
    "cypress": "^3.1.3",
    "uuid": "^3.3.2"
  },
  "devDependencies": {
    "cypress-failed-log": "^2.4.0",
    "eslint": "^5.10.0",
    "eslint-config-edx": "^4.0.4",
    "eslint-plugin-cypress": "^2.1.2"
  }
}
